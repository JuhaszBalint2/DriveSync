<Window x:Class="DriveSync.WPF.Views.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available" 
        Height="250" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Rclone Update" 
                   Style="{DynamicResource SubheadingStyle}"
                   FontSize="18"
                   Margin="0,0,0,10"
                   Grid.Row="0"/>

        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="A new version of Rclone is available."
                       Style="{DynamicResource BodyTextStyle}"
                       Margin="0,0,0,10"/>
            <TextBlock Text="{Binding UpdateMessage}"
                       Style="{DynamicResource BodyTextStyle}"
                       Margin="0,0,0,20"/>
            <ProgressBar IsIndeterminate="True" 
                         Height="10" 
                         Margin="0,0,0,10"/>
            <TextBlock Text="Please wait while the update is being downloaded and installed..."
                       Style="{DynamicResource BodyTextStyle}"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>